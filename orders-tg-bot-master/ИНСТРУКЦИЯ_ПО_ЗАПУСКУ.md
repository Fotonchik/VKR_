# Инструкция по запуску Telegram бота

## Что было исправлено:

1. ✅ Исправлен путь к базе данных в `config.py` - теперь используется относительный путь
2. ✅ Переписан `db.py` - теперь только функции БД без кода бота
3. ✅ Исправлен `admin_bot.py` - убран `bot.infinity_polling()`, сделан модулем
4. ✅ Исправлен `operator_manager_bot.py` - убраны конфликты, сделан модулем
5. ✅ Исправлен `main_bot.py` - правильно интегрированы все модули
6. ✅ Добавлены недостающие функции в `format.py`
7. ✅ Исправлен `db_tickets.py` - используется единый путь к БД
8. ✅ Добавлена библиотека `fpdf2` в requirements.txt

## Установка зависимостей:

1. Убедитесь, что у вас установлен Python 3.7 или выше
2. Откройте терминал в папке проекта
3. Создайте виртуальное окружение (если еще не создано):

```bash
python -m venv venv
```

4. Активируйте виртуальное окружение:

**На Windows:**
```bash
venv\Scripts\activate
```

**На Linux/Mac:**
```bash
source venv/bin/activate
```

5. Установите зависимости:

```bash
pip install -r requirements.txt
```

**ВАЖНО**: Если при установке возникает ошибка о Microsoft Visual C++ Build Tools (часто бывает на Windows), выполните:

```bash
pip install pyTelegramBotAPI fpdf2
```

Это установит только необходимые пакеты с готовыми wheel-файлами, не требуя компиляции.

## Настройка конфигурации:

Проверьте файл `config.py` и убедитесь, что:

1. **BOT_TOKEN** - ваш токен бота от @BotFather
2. **ADMIN_CHAT_ID** - список ID администраторов (например: `[950904592]`)
3. **TP_CHAT_ID** - список ID операторов
4. **MANAGER_CHAT_ID** - список ID менеджеров

**ВАЖНО**: Токен бота уже указан в config.py. Убедитесь, что он актуален и действителен.

## Запуск бота:

### Вариант 1: Использование готового batch-файла (Windows) - РЕКОМЕНДУЕТСЯ

Просто дважды кликните на файл `start_bot.bat` в папке проекта. Он автоматически:
- Проверит наличие виртуального окружения
- Активирует его
- Установит зависимости при необходимости
- Запустит бота

### Вариант 2: Запуск через командную строку

**Шаг 1**: Активируйте виртуальное окружение

**На Windows:**
```bash
venv\Scripts\activate
# Или с точкой:
. venv/Scripts/activate
```

**На Linux/Mac:**
```bash
source venv/bin/activate
```

**Шаг 2**: Запустите бота

```bash
python main_bot.py
```

Вы должны увидеть сообщение: `🚀 Запущен универсальный бот`

### Вариант 3: Запуск в фоновом режиме (Windows)

```powershell
Start-Process python -ArgumentList "main_bot.py" -WindowStyle Hidden
```

## Структура проекта:

- `main_bot.py` - главный файл запуска бота
- `config.py` - конфигурация (токен, ID пользователей)
- `db.py` - функции работы с основной БД (пользователи, заказы)
- `db_tickets.py` - функции работы с БД заявок
- `admin_bot.py` - функции административной панели
- `operator_manager_bot.py` - функции панелей оператора и менеджера
- `format.py` - функции форматирования текста
- `orders.db` - база данных SQLite (создастся автоматически)

## Команды бота:

- `/start` - запуск бота, автоматическое определение роли и показ соответствующей панели
- `/admin` - открыть административную панель (только для администраторов)

## Роли пользователей:

1. **Администратор** - полный доступ, управление пользователями и заявками
2. **Менеджер** - просмотр и обработка заявок от операторов
3. **Оператор (TP)** - работа с заявками, создание комментариев

## Проверка работы:

1. Убедитесь, что бот запущен (должно появиться сообщение "🚀 Запущен универсальный бот")
2. Найдите вашего бота в Telegram
3. Отправьте команду `/start`
4. В зависимости от вашего ID в config.py, вы должны увидеть соответствующую панель

## Решение проблем:

### Ошибка "ModuleNotFoundError: No module named 'telebot'"
**Решение**: Установите зависимости: `pip install -r requirements.txt`

### Ошибка "Invalid token"
**Решение**: Проверьте BOT_TOKEN в config.py, получите новый токен у @BotFather

### Ошибка "Permission denied" при создании БД
**Решение**: Убедитесь, что у вас есть права на запись в папку проекта

### Бот не отвечает
**Решение**: 
- Проверьте интернет-соединение
- Убедитесь, что бот запущен (не было ошибок при запуске)
- Проверьте логи в консоли

### Ошибка "Conflict: terminated by other getUpdates request" (409)
**Причина**: Запущено несколько экземпляров бота одновременно. Telegram API позволяет только один активный экземпляр бота.

**Решение**: 
1. Остановите все запущенные экземпляры бота (нажмите `Ctrl+C` во всех окнах терминала)
2. Или используйте файл `stop_bot.bat` для автоматической остановки всех процессов Python
3. Подождите 5-10 секунд
4. Запустите бота снова командой `python main_bot.py`

**Проверка запущенных процессов:**
```bash
tasklist | findstr python
```

Если видите несколько процессов `python.exe`, остановите их все перед запуском бота.

### Ошибка SSL (SSLError)
**Решение**: 
- Проверьте интернет-соединение
- Отключите VPN/прокси, если используется
- Проверьте настройки файрвола/антивируса
- Убедитесь, что доступен сайт api.telegram.org

## Остановка бота:

Нажмите `Ctrl+C` в терминале, где запущен бот.

## База данных:

База данных `orders.db` создается автоматически при первом запуске. Она содержит:
- Таблицу пользователей (users)
- Таблицу товаров (products)
- Таблицу заказов (orders)
- Таблицу заявок (tickets)
- Таблицу клиентов (clients)
- Таблицу комментариев (ticket_comments)

## Дополнительные замечания:

- Бот использует HTML-форматирование для сообщений
- Все данные хранятся в локальной SQLite базе данных
- При добавлении нового администратора используйте функцию в админ-панели
- ID пользователя можно узнать через бота @myidbot в Telegram

